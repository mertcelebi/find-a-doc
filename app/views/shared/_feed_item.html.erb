<li id="<%= feed_item.id %>">
  <span class="content">
    <%= p "You have searched for" %>
    <%= feed_item.symptom %>
    <%= feed_item.icd_9 %>
    <%= feed_item.specialty %>
    <%= p "in" %>
    <%= feed_item.address %>
    <%= feed_item.city %>
    <%= feed_item.state %>
    <%= p "-" %>
    <%= feed_item.zipcode %>
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(feed_item.created_at) %> ago.
  </span>
   <% if current_user?(feed_item.user) %>
    <%= link_to "Delete", feed_item, method: :delete,
                                     data: { confirm: "Are you sure?" },
                                     title: feed_item.zipcode %>
  <% end %>
</li>